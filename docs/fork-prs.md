# Fork PR Guidance

- Use `pull_request_target` **only** if you understand the security tradeoffs.
- Prefer updating the badge on `push` to protected branches.
- For fork PRs, avoid `contents: write` unless required.
